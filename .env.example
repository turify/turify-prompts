# ==============================================================================
# CORE REQUIRED VARIABLES
# These are essential for the application to run properly
# ==============================================================================

# Database Configuration (REQUIRED)
# Your primary database connection string - app won't work without this
DATABASE_URL="your-production-database-url-here"

# Development Database (REQUIRED for development)
# Separate database URL for development environment
DATABASE_URL_DEV_DATABASE_URL="your-dev-database-url-here"

# NextAuth Configuration (REQUIRED)
# Secret key for NextAuth.js authentication - generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret"

# Application URL (REQUIRED)
# The base URL where your application is hosted
NEXT_PUBLIC_URL="localhost:3000"

# ==============================================================================
# AUTHENTICATION (Optional - for Google OAuth)
# Only required if you want to enable Google sign-in
# ==============================================================================

# Google OAuth Credentials
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ==============================================================================
# EMAIL CONFIGURATION (Optional - for transactional emails)
# Required only if you want to send emails (welcome, notifications, etc.)
# ==============================================================================

# Email Server Settings
# Configure based on your email provider (Gmail, AWS SES, etc.)
EMAIL_SERVER_HOST="your-email-server-host"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email-server-user"
EMAIL_SERVER_PASSWORD="your-email-server-password"
EMAIL_FROM="your-from-email@example.com"

# ==============================================================================
# AI/ML SERVICES (Optional - for AI features)
# Only required if you're using AI-powered features
# ==============================================================================

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key"

# LangChain Configuration
# For LangSmith tracing and monitoring
LANGCHAIN_API_KEY="your-langchain-api-key"
LANGCHAIN_PROJECT="your-langchain-project-name"

# ==============================================================================
# SECURITY & MISC (Optional)
# Additional security and utility configurations
# ==============================================================================

# Unsubscribe Secret
# Used for generating secure unsubscribe links in emails
UNSUBSCRIBE_SECRET="your-unsubscribe-secret-key-here"
